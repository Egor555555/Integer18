#include "pt4.h"
using namespace std;

void Solve()
{
Task("If22");
float x, y;
pt » x » y;
if (x > 0 && y > 0) {
pt « 1;
}
else if (x < 0 && y>0) {
pt « 2;
}else if (x<0 && y<0){
pt « 3;
}
else if (x > 0 && y < 0) {
pt « 4;
}

}
